# PRE_CONVERSION

Finished one-time work, kept unmodified for the record. **Nothing here is needed to
install or use Desktop Dashboard** — start at [`../README.md`](../README.md) and
[`../INSTALL.md`](../INSTALL.md).

This folder was called `archive/` until 2026-08-03, when the repo was migrated onto the
project spine and it was renamed to the standard's name for exactly this
(`../DECISIONS.md` **D63**). **The two files below are the originals and were not
edited** — only this index was updated, to say the new folder name.

- **`MOVING.md`** — the brief for turning a working single Lua file into this repo. Done on
  2026-07-27; it describes a state that no longer exists.
- **`STATUS.md`** — what that migration actually did, including two detection bugs it
  surfaced and why the Claude app Desktop ended up with a manual name. **This is a
  one-time record from 2026-07-27, not a living snapshot** — it is not the same kind of
  file as [`../STATUS.md`](../STATUS.md), which is why it stayed here rather than being
  merged into it.

Both are superseded as *instructions*. They are kept because they record decisions and
measurements that the current docs state as conclusions — useful if you ever want to know
why something is the way it is. The living version of that reasoning is
[`../DECISIONS.md`](../DECISIONS.md), with [`../CLAUDE.md`](../CLAUDE.md) for the
architecture.
